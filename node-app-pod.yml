apiVersion: v1
kind: Pod
metadata:
  name: lms
  labels:
    app: lms
spec:
  containers:
    - name: lms
      image: keshavmurali/lms-app:latest
      ports:
        - containerPort: 80
          name: lms
